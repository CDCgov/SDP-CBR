#spring.main.sources=org.mycompany

logging.config=classpath:logback.xml

# the options from org.apache.camel.spring.boot.CamelConfigurationProperties can be configured here
camel.springboot.name=SDP-CBR

# lets listen on all ports to ensure we can be invoked from the pod IP
server.address=0.0.0.0
management.address=0.0.0.0

# lets use a different management port in case you need to listen to HTTP requests on 8080
management.port=8081

# disable all management endpoints except health
endpoints.enabled = false
endpoints.health.enabled = true
# lets use a different management port in case you need to listen to HTTP requests on 8080
management.port=8081

# disable all management endpoints except health
endpoints.enabled = false
endpoints.health.enabled = true


foodNet.queue=queue:foodNet?acknowledgementModeName=CLIENT_ACKNOWLEDGE
activemq.url=vm://localhost?broker.persistent=false&broker.useJmx=false

aims.bucketName=foodnet
aims.AccessKey=accessKey1
aims.SecretAccessKey=verySecretKey1
aims.S3Url=http://127.0.0.1:8000
aims.url=foodnet?amazonS3Endpoint=http://127.0.0.1:8000&accessKey=accessKey1&secretKey=verySecretKey1
aims.url2=${aims.bucketName}?amazonS3Endpoint=${aims.S3Url}&accessKey=${aims.accessKey}&secretKey=${aims.SecretAccessKey}
aims.SQSUrl=
aims.SQSNotificationURL=